---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<!-- CRT Monitor Wrapper for Desktop -->
<div class="md:fixed md:inset-0 bg-black md:flex md:items-center md:justify-center min-h-screen z-0">
  <!-- The CRT Bezel -->
  <div class="w-full min-h-screen md:min-h-0 md:h-[85vh] md:w-[90vw] md:max-w-5xl md:rounded-[3rem] md:border-[24px] md:border-[#1a1a24] md:shadow-[0_20px_60px_rgba(0,0,0,0.9),inset_0_0_30px_rgba(0,0,0,1)] relative overflow-hidden mx-auto bg-[#0a0a0f] flex flex-col">
    <!-- Inner Monitor Screen Glass/Shadow -->
    <div class="hidden md:block absolute inset-0 pointer-events-none md:rounded-[1.5rem] shadow-[inset_0_0_100px_rgba(0,0,0,0.95)] z-[70]"></div>
    
    <div id="terminal" class:list={["flex-1 min-h-screen md:min-h-0 bg-[#0a0a0f] text-[#e2e8f0] flex flex-col grid-bg monitor-warp scanlines relative", className]}>
  <!-- Construction Tape -->
  <div class="w-full h-8 construction-tape flex items-center justify-center z-[60] shadow-[0_4px_15px_rgba(0,0,0,0.8)] border-b-2 border-black relative">
    <div class="bg-black px-6 py-0.5 text-[#facc15] font-mono font-bold text-xs md:text-sm tracking-widest flex items-center gap-4 whitespace-nowrap whitespace-pre">
      <span>🚧</span>
      <span>SITE UNDER CONSTRUCTION</span>
      <span>🚧</span>
    </div>
  </div>

  <div class="scanline-moving"></div>
  
  <!-- Terminal Output Area -->
  <div id="terminal-output" class="flex-1 overflow-y-auto p-4 pb-[140px] font-mono text-sm md:text-base w-full max-w-4xl mx-auto relative z-10">
    <!-- Welcome Message -->
    <div class="mb-6">
      <div class="text-[#39ff14] font-bold text-lg mb-2">Welcome to bigf.me</div>
      <div class="text-[#ff6b9d] text-sm mb-1">Type 'help' or click a quick button below</div>
    </div>
  </div>

  <!-- Bottom Control Area -->
  <div class="absolute bottom-0 left-0 right-0 flex flex-col z-50">
    <!-- Quick Buttons Bar -->
    <div class="bg-[#15151f]/95 backdrop-blur-sm border-t border-[#2a2a3f] px-4 py-2">
      <div class="flex items-center gap-2 max-w-4xl mx-auto overflow-x-auto scrollbar-hide">
        <span class="text-[#6b7280] text-xs shrink-0 mr-1">Quick:</span>
        
        <!-- HIGHLIGHTS Button - Special Glowing Animation -->
        <button 
          class="highlight-btn relative px-3 py-1.5 bg-gradient-to-r from-[#ff6b9d]/20 via-[#b829dd]/20 to-[#00d4ff]/20 border border-[#ff6b9d] rounded text-xs text-[#ff6b9d] font-bold tracking-wider overflow-hidden group shrink-0" 
          data-cmd="highlights"
        >
          <span class="relative z-10 flex items-center gap-1">
            <span class="animate-pulse">✨</span>
            HIGHLIGHTS
            <span class="animate-pulse">✨</span>
          </span>
          <div class="absolute inset-0 bg-gradient-to-r from-[#ff6b9d] via-[#b829dd] to-[#00d4ff] opacity-0 group-hover:opacity-30 transition-opacity"></div>
          <div class="absolute -inset-1 bg-gradient-to-r from-[#ff6b9d] via-[#b829dd] to-[#00d4ff] opacity-20 blur-sm animate-glow-pulse"></div>
        </button>
        
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="whoami">whoami</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="ls">ls</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="cat about">about</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#39ff14]/50 rounded text-xs text-[#39ff14] hover:border-[#39ff14] hover:bg-[#39ff14]/10 transition-all duration-200 shrink-0" data-cmd="cat life">🎨 life</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="cat books">books</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="cat philosophy">philosophy</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#2a2a3f] rounded text-xs text-[#e2e8f0] hover:border-[#00d4ff] hover:text-[#00d4ff] transition-all duration-200 shrink-0" data-cmd="cat now">now</button>
        <button class="quick-btn px-2 py-1 bg-[#15151f] border border-[#ff6b9d] rounded text-xs text-[#ff6b9d] hover:bg-[#ff6b9d] hover:text-[#0a0a0f] transition-all duration-200 shrink-0 ml-auto" data-cmd="clear">clear</button>
      </div>
    </div>

    <!-- Active Prompt - Attached to very bottom -->
    <div class="bg-[#0a0a0f]/95 backdrop-blur-sm border-t border-[#2a2a3f] p-3">
      <div class="flex items-center gap-2 max-w-4xl mx-auto">
        <span class="text-[#ff6b9d] shrink-0 font-semibold">bigseed@bigf.me</span>
        <span class="text-[#6b7280]">:</span>
        <span id="current-path" class="text-[#00d4ff]">~</span>
        <span class="text-[#e2e8f0]">$</span>
        <input
          id="terminal-input"
          type="text"
          class="flex-1 bg-transparent text-[#e2e8f0] outline-none font-mono text-sm md:text-base min-w-0 caret-[#00d4ff]"
          autocomplete="off"
          autocorrect="off"
          autocapitalize="off"
          spellcheck="false"
        />
      </div>
    </div>


  </div>
    </div>
  </div>
</div>

<style>
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  @keyframes tape-scroll {
    from { background-position: 0 0; }
    to { background-position: -56.57px 0; }
  }

  .construction-tape {
    background: repeating-linear-gradient(
      -45deg,
      #facc15,
      #facc15 20px,
      #000000 20px,
      #000000 40px
    );
    background-size: 56.57px 56.57px;
    animation: tape-scroll 2s linear infinite;
  }
</style>

<script>
  import { executeCommand, projects, categories } from '../lib/commands.ts';
  import { createProjectCard, createCarousel } from './ProjectCard.ts';
  import type { Project } from '../lib/commands.ts';

  const terminal = document.getElementById('terminal-output');
  const input = document.getElementById('terminal-input') as HTMLInputElement;
  const currentPathEl = document.getElementById('current-path');

  let currentPath = '~';
  let commandHistory: string[] = [];
  let historyIndex = -1;

  function scrollToBottom() {
    if (terminal) {
      requestAnimationFrame(() => {
        terminal.scrollTop = terminal.scrollHeight;
        setTimeout(() => {
          terminal.scrollTop = terminal.scrollHeight;
        }, 50);
      });
    }
  }

  function addCommandToOutput(command: string) {
    const cmdLine = document.createElement('div');
    cmdLine.className = 'mb-2';
    cmdLine.innerHTML = `
      <span class="text-[#ff6b9d] font-semibold">bigseed@bigf.me</span>
      <span class="text-[#6b7280]">:</span>
      <span class="text-[#00d4ff]">${currentPath}</span>
      <span class="text-[#e2e8f0]">$</span>
      <span class="text-[#e2e8f0] ml-2">${command}</span>
    `;
    terminal?.appendChild(cmdLine);
    scrollToBottom();
  }

  function addOutputToTerminal(output: string, isError = false, element?: HTMLElement) {
    if (output === '__CLEAR__') {
      if (terminal) {
        terminal.innerHTML = '';
        const welcome = document.createElement('div');
        welcome.className = 'mb-6';
        welcome.innerHTML = `
          <div class="text-[#39ff14] font-bold text-lg mb-2">Welcome to bigf.me</div>
          <div class="text-[#ff6b9d] text-sm mb-1">Type 'help' or click a quick button below</div>
        `;
        terminal.appendChild(welcome);
      }
      return;
    }

    if (element) {
      terminal?.appendChild(element);
    } else {
      const outputDiv = document.createElement('div');
      outputDiv.className = `mb-4 whitespace-pre-wrap ${isError ? 'text-[#ff6b9d]' : 'text-[#e2e8f0]'}`;
      
      if (isError) {
        outputDiv.textContent = output;
      } else {
        let formattedOutput = output
          // Escape HTML first
          .replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;")
          // Horizontal rule
          .replace(/^---$/gm, '<span class="text-[#2a2a3f] block my-2">----------------------------------------</span>')
          // Headers
          .replace(/^###\s+(.+)$/gm, '<span class="text-[#b829dd] font-bold text-md block mt-2 mb-1">### $1</span>')
          .replace(/^##\s+(.+)$/gm, '<span class="text-[#00d4ff] font-bold text-lg block mt-3 mb-1">## $1</span>')
          .replace(/^#\s+(.+)$/gm, '<span class="text-[#39ff14] font-bold text-xl block mt-4 mb-2"># $1</span>')
          // Blockquotes
          .replace(/^&gt;\s+(.+)$/gm, '<span class="text-[#a78bfa] italic border-l-2 border-[#a78bfa] pl-2 block my-2">&gt; $1</span>')
          // Bold
          .replace(/\*\*(.*?)\*\*/g, '<span class="font-bold text-[#facc15]">$1</span>')
          // Italic
          .replace(/(^|[^\*])\*([^\*]+)\*(?!\*)/g, '$1<span class="italic text-[#a78bfa]">$2</span>')
          // Lists (- item)
          .replace(/^-\s+(.+)$/gm, '<span class="text-[#ff6b9d]">-</span> <span class="text-[#e2e8f0]">$1</span>')
          // Links
          .replace(/\[([^\]]+)\]\(([^)]+)\)/g, '<a target="_blank" href="$2" class="text-[#00d4ff] underline hover:text-[#ff6b9d] transition-colors">$1</a>')
          // Directories permissions
          .replace(/^drwxr-xr-x\s+(.+)$/gm, '<span class="text-[#6b7280]">drwxr-xr-x</span>  <span class="text-[#00d4ff] font-bold">$1</span>')
          // File permissions
          .replace(/^-rw-r--r--\s+(.+)$/gm, '<span class="text-[#6b7280]">-rw-r--r--</span>  <span class="text-[#e2e8f0]">$1</span>');
          
        outputDiv.innerHTML = formattedOutput;
      }
      
      terminal?.appendChild(outputDiv);
    }
    scrollToBottom();
  }

  function renderProjectCards(projectList: string[], categoryName?: string) {
    if (projectList.length === 0) return null;
    
    const projectData = projectList
      .map(id => ({ ...projects[id], id }))
      .filter(p => p.name);
    
    const cards = projectData.map(p => 
      createProjectCard({
        project: p,
        onClick: (projectId) => {
          const cmd = categoryName ? `cd ${categoryName}/${projectId}` : `cd ${projectId}`;
          execute(cmd);
          execute('ls');
        }
      })
    );
    
    return createCarousel({
      title: categoryName ? `${categoryName} projects` : 'Projects',
      items: cards
    });
  }

  function execute(cmd: string) {
    if (!cmd.trim()) return;

    commandHistory.push(cmd);
    historyIndex = commandHistory.length;

    addCommandToOutput(cmd);

    const parts = cmd.trim().split(' ');
    const command = parts[0].toLowerCase();
    const args = parts.slice(1);

    // Handle highlights command - show curated best projects (optimized for Eigen)
    if (command === 'highlights') {
      const highlightProjects = ['passionseed', 'fync', 'longkeeb', 'hive', 'careerac'];
      const carousel = renderProjectCards(highlightProjects);
      if (carousel) {
        addOutputToTerminal('', false, carousel);
      }
      addOutputToTerminal(`✨ HIGHLIGHTS

These are the projects I'm most proud of:

• PassionSeed - AI career discovery for students (300 hackathon users)
• Fync - Friendship graph API (early exploration in relationship modeling)
• LongKeeb/Portex - Hardware keyboards I designed and sold
• Hive - AI democracy tool (Gemini hackathon semi-finalist)
• CareerAc - Career planning AI (Berkeley Challenge Lab)

Click any card to explore, or use 'cd [project]' to navigate deeper.

Type 'cat life' for music, places, and art.`);
      input.value = '';
      scrollToBottom();
      return;
    }

    // Handle ls with visual cards
    if (command === 'ls') {
      const target = args[0] || currentPath;
      
      // Home - show all categories with cards
      if (target === '~' || target === '/' || target === 'home') {
        // Show categories as cards
        const categoryCards = Object.entries(categories).map(([cat, projs]) => {
          const card = document.createElement('div');
          card.className = 'flex-shrink-0 w-48 bg-[#15151f] border border-[#2a2a3f] rounded-lg p-4 cursor-pointer hover:border-[#ff6b9d] transition-all group';
          card.innerHTML = `
            <div class="text-2xl mb-2">${getCategoryIcon(cat)}</div>
            <h3 class="text-sm font-semibold text-[#e2e8f0] group-hover:text-[#ff6b9d] transition-colors capitalize">${cat}</h3>
            <p class="text-xs text-[#6b7280] mt-1">${projs.length} projects</p>
          `;
          card.addEventListener('click', () => {
            execute(`cd ${cat}`);
            execute('ls');
          });
          return card;
        });
        
        const carousel = createCarousel({ title: 'Categories', items: categoryCards });
        addOutputToTerminal('', false, carousel);
        
        // Also show text output
        addOutputToTerminal(`drwxr-xr-x  categories/
drwxr-xr-x  pages/

Use 'cd [category]' to explore or click a card above.`, false);
      } 
      // Category - show projects as cards
      else {
        const category = target.split('/')[0];
        const projectList = categories[category];
        if (projectList) {
          const carousel = renderProjectCards(projectList, category);
          if (carousel) {
            addOutputToTerminal('', false, carousel);
          }
          // Also show text list
          const items = projectList.map(p => `drwxr-xr-x  ${p}/`).join('\n');
          addOutputToTerminal(`${category}/:\n${items}\n\nClick a card above or use 'cd [project]' to navigate.`);
        } else {
          addOutputToTerminal(`ls: ${target}: No such directory`, true);
        }
      }
      
      // Update path if needed
      const result = executeCommand(cmd, currentPath);
      if (result.newPath !== undefined) {
        currentPath = result.newPath;
        if (currentPathEl) currentPathEl.textContent = currentPath;
      }
      input.value = '';
      return;
    }

    // Default command execution
    const result = executeCommand(cmd, currentPath);

    if (command === 'gallery') {
      addOutputToTerminal(result.output, result.error);
      setTimeout(() => {
        window.location.href = '/life';
      }, 1000);
      return;
    }

    addOutputToTerminal(result.output, result.error);

    if (result.newPath !== undefined) {
      currentPath = result.newPath;
      if (currentPathEl) currentPathEl.textContent = currentPath;
    }

    input.value = '';
  }

  function getCategoryIcon(category: string): string {
    const icons: Record<string, string> = {
      hardware: '🔧',
      education: '📚',
      ai: '🤖',
      sustainability: '🌱',
      community: '👥',
      experiments: '🧪'
    };
    return icons[category] || '📁';
  }

  input?.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') {
      execute(input.value);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      if (historyIndex > 0) {
        historyIndex--;
        input.value = commandHistory[historyIndex] || '';
      }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault();
      if (historyIndex < commandHistory.length - 1) {
        historyIndex++;
        input.value = commandHistory[historyIndex] || '';
      } else {
        historyIndex = commandHistory.length;
        input.value = '';
      }
    }
  });



  const quickBtns = document.querySelectorAll('.quick-btn');
  quickBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const cmd = btn.getAttribute('data-cmd') || '';
      execute(cmd);
    });
  });

  // Also attach click handler to highlight-btn (HIGHLIGHTS button)
  const highlightBtns = document.querySelectorAll('.highlight-btn');
  highlightBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const cmd = btn.getAttribute('data-cmd') || '';
      execute(cmd);
    });
  });

  // Always focus input on load
  input?.focus();
  
  // Auto-copy selection on mouseup
  document.addEventListener('mouseup', async () => {
    const selection = window.getSelection()?.toString();
    if (selection) {
      try {
        await navigator.clipboard.writeText(selection);
      } catch (err) {
        console.error('Failed to copy text', err);
      }
    }
  });

  // Focus input on click, but only if they aren't selecting text
  document.addEventListener('click', () => {
    const selection = window.getSelection()?.toString();
    if (!selection) {
      input?.focus();
    }
  });

  setTimeout(scrollToBottom, 100);
</script>
